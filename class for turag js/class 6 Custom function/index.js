// console.log("its work");

// Math.random()

// Custom function
var meal = 100;
var textRate = 0.5;
function calculateBill() {
  // function body
  const total = meal * (1 + textRate);
  return total;
}

const myToatl = calculateBill();
// console.log(myToatl);

//  == === > <

// aria pi* r*r
// const pi = Math.PI;
// const radiaus = 6;

function calculateArea(radius, pi) {
  const area = pi * radius ** 2;
  // const name = "sakib";
  // console.log(name);
  return area;
}

const areaTotal = calculateArea();
console.log(areaTotal);
